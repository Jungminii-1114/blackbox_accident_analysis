# [실험 리뷰] portfolio_v1 — 공식 S3 0.57137, S2 0.16320 / 다음 실험은 S1·2 집중

## 요약

사용자가 `portfolio_v1_20260910T063010Z` 결과로 전달한 리더보드 화면 기준:

| Stage | 원래 baseline | 이번 공식 결과 | 변화(약) |
|---|---:|---:|---:|
| S1 | 0.5339789355 | 0.52121 | -0.01277 |
| S2 | 0.1172182028 | 0.16320 | +0.04598 |
| S3 | 약 0.14581 | 0.57137 | +0.42556 |
| 가중 합계(0.2/0.4/0.4) | 약 0.21201 | 약 0.39807 | +0.18606 |

이번 값은 화면의 반올림된 Stage 점수다. 최종 Private 점수/순위가 아니며, 표시된 순위 135는
Stage 1 정렬 화면의 값으로 전체 종합 순위라고 해석하지 않는다. 제출수는 3으로 보이며
남은 제출 기회가 있다고 가정하지 않는다. 화면과 실험의 연결은 사용자 설명에 근거한다.

## 무엇을 제출했나

- S1: 기존 MViTv2-S checkpoint, 3개 대신 5개 clip 확률 평균.
- S2: 기존 모델의 진입시점·방향·회피공간 유지. 충돌시점만 차영상/전역 이동 변화 B로 교체.
- S3: Farneback 도로/배경 flow A + 공개 라벨 기반 임계값 분류.
  임계값 `[1.165121976286173, 2.930227120717365, 2.346569240093232]`.
- 외부 CAN head 재학습 없이 공개 source-group 검증으로 새 후보를 비교.

검증된 제출 산출물의 기록:

- ZIP SHA-256: `96da8cb8593ae76867ff53c43b6e0159495172f73cc3f6f0c0f44ef9145b33c0`
- inference.py SHA-256: `bc71a3093549edc2d50a1317aad3dd486e380bc0532546b5ad2cde1e6e709b55`
- Colab: Python 3.13.15 / Torch 2.11.0+cu128 / TorchVision 0.26.0+cu128 / OpenCV 5.0.0 / T4.
- 예제 전체 추론: workspace 약 84.8초, ZIP 추출본 약 81.6초.

이는 Colab 로그/해시 기록이며 실제 채점된 서버의 설치 패키지 버전까지 확인한 것은 아니다.

## 로컬 결과와 공식 결과가 어떻게 달랐나

### S1: 로컬 동률이 공식 안전성을 보장하지 않았다

baseline/5-clip/median 모두 공개 10영상을 전부 ORIGINAL로 예측했다. Macro-F1은 0.33333,
예측 클래스의 구분 개선이 없었다. 공식 점수는 약 0.01277 하락했다.
다음 실험에서는 같은 모델의 clip 수만 늘리는 대신 frozen ResNet18 특징의 작은 분류 head를 비교한다.

### S2: 작은 공개셋의 퇴행이 공식 퇴행을 뜻하지 않았다

Colab에서 baseline과 B 모두 Accuracy@0.3초는 5/5였다. 그러나 MAE는 0.2→1.8프레임
(예제 10Hz 기준 0.02→0.18초)으로 나빠졌다. Mac에서는 B가 3/5였고 재압축에도 예측이 바뀌었다.
그럼에도 이번 공식 Stage 2는 0.11722→0.16320으로 상승했다.

이전 어시스턴트의 “S2를 baseline으로 되돌리는 것이 낫다”는 추천은 이번 공식 상승을 예상하지 못했다.
공개 5개 예제의 정확도·MAE만으로 숨은 데이터에 대한 우열을 단정하면 안 된다.
이제 공식 개선된 B를 새 비교 기준으로 보존한다. 민감도 문제 자체가 사라진 것은 아니므로
후속 후보는 MAE와 JPEG95/90 안정성도 함께 검증한다.

### S3: 영상 운동 가설이 이번 공식 평가에서 개선을 보였다

| 측정 | 점수 | 의미 |
|---|---:|---|
| 기존 모델, 이번 로컬 시간 정렬 | 0.14014 | 공개 baseline 비교 |
| A/B 선택 절차 OOF | 0.47851 | outer 5 fold에서 모두 B가 선택된 절차의 성능 |
| A만 사용한 별도 OOF | 0.48510 | 최종 A 계열의 참고 검증 |
| 전체 데이터 적합 후 같은 데이터 평가 | 0.62162 | 일반화 검증으로 사용 금지 |
| 공식 Stage 3 | 0.57137 | 사용자 전달 화면의 관측값 |

최종 A의 OOF 가감속 Macro-F1=0.35768, 조향=0.78242. 감속 F1=0.10526으로 아직 약하다.
이번 한 번의 성공으로 로컬 점수를 공식 점수로 환산하는 공식을 만들지 않는다.
이전 CAN head의 음의 전이 사례까지 고려하면 후보 종류·라벨·도메인에 따라 관계가 달라진다.

## 검증·재현성에서 확인한 것

- 보고서/CSV 해시가 package_check 기록과 일치.
- workspace/ZIP의 S1 10행, S2 5행, S3 2,998행 예측 일치.
- S2에서 충돌 외 세 출력이 부모와 동일.
- 공개 S3 MP4 컨테이너가 약 480fps/2.5초로 표시되는 문제 확인.
  공개 라벨의 frame/time 대응(20Hz)을 검사해 짝수 프레임을 10Hz fixture로 변환했다.
  50개 공개 라벨 시점의 변환 후 시간 오차는 0초였다.
- 로컬·Colab은 같은 코드와 원본 영상을 사용했지만 S2 JPEG 250개 및 S3 변환 영상의 파일 해시가 달랐다.
  파일 바이트 차이만으로 픽셀 차이와 알고리즘 버전 영향의 기여를 분리할 수는 없다.

## 다음 실험: focus12_v2

1. 실제 제출 ZIP을 부모로 읽어 hash 고정. S3 코드/임계값을 그대로 보존한다.
2. S1: 기존 ResNet18 고정 특징 + ridge head A/B. 원본/재촬영 5쌍을 그룹으로 nested CV.
3. S1 채택 조건: 선택 절차 및 최종 계열 OOF ≥ max(0.60, baseline+0.10), JPEG90 일치율 ≥0.90,
   두 클래스 모두 출력. 미달 시 원래 공식 성적이 더 좋았던 3-clip으로 복구.
4. S2: E=부모 B 앞뒤 2프레임 국소 정밀화, C=노출 보정 차영상/phase shift 순위 결합, D=차영상 시간적 지지.
   공식 B 대비 정확도 비열화, MAE ≥0.02초 개선, JPEG95/90 검사 통과 시만 변경. 아니면 B 유지.
5. 결합 ZIP을 두 번 추론하고 S3의 현재 부모 출력과 정확히 일치하는지 전수 비교.

원래 S1 + 이번 S2/S3의 산술적 조합은 약 0.40062지만 **아직 실제 관측한 제출 점수가 아니다**.
최종 GO는 탐색/조합 실행 가능 판정이며 공식 상승 확률은 N/A로 둔다.

## 완료 조건 및 남은 일

- [x] 공식 화면 반영, 이전 추천의 한계 기록
- [x] 로컬 점수/예측/해시 대조
- [x] 후속 Colab 노트북·소형 patch 구현
- [ ] Colab에서 새 S1·S2 후보 및 압축 민감도 실험 실행
- [ ] 부모 S3 전수 동등성, ZIP import/재추론 검사 통과
- [ ] 제출 가능 횟수 확인 후 필요한 경우에만 공식 제출
- [ ] 실제 재촬영/사고 라벨을 추가 확보해 독립 검증셋 확장

실행 파일: `05_focus12_v2.ipynb`, `dacon236753_focus12_patch.zip`.
실행 안내: `docs/focus12_v2_run_guide.md`.
